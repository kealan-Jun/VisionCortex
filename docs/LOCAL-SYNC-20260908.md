# 2026-09-08 本地优化同步与 RTX 4050 后续工作

用户明确要求将本地优化提交到 `RealityLoopAI/VisionCortex`，并建立 RTX 4050
优化分支。本次按开发快照同步分支，先向 development 推送，再向指定仓库推送
同一提交；不更新稳定 `main`、标签或 Release，也不宣称通过稳定晋升门禁。
这是本次用户指定的分支协作方式，不是已通过双仓稳定发布验收的记录。

## 快照身份与范围

- 来源分支：`codex/rtx3050-device-delivery-20260904`。
- 来源基础提交：`7db0dcd052891e0e189c5e931a27558256fea032`。
- 同步分支：`codex/local-optimizations-20260908`。
- 后续分支：`codex/rtx4050-optimization-20260908`，从同步提交建立。
- 冻结时收集 164 个本地变更文件，包含应用、Web、厂商接入、素材与阶段结果、
  语音、视觉改动、训练工具、部署脚本、测试及说明。提交另外包含本说明，并调整
  一个测试中的合成凭据占位值，避免凭据模式扫描误报。
- 模型权重、引擎、视频、运行产物、缓存、真实凭据及测试生成的 `D:/` 均不加入提交。
- 使用独立 Git worktree；原工作区的分支和未提交文件保留，其他任务可以继续工作。

## 验证与已知边界

本地完整确定性回归：**1417 passed、4 skipped、1 failed**。
失败项为 `test_dependency_boundaries.py::test_heavy_model_imports_remain_lazy_in_production_modules`，
命中新加入的训练模块顶层重型依赖导入。这是快照中已有的依赖边界问题，未借同步
操作调整训练行为，也未把它记为通过。26 项桌面 Node 合同测试通过；Ruff、编译、
Web JavaScript 语法、已变更 Ubuntu 脚本语法和 diff 空白检查通过。

当前提交文件的凭据模式扫描通过。可达历史中有两份旧模型 blob，已存在于指定仓库
的 `main` 历史；另有一个旧 `review_backend` 标识符被模式扫描命中，它不是凭据。
本次没有重写历史或新增模型 blob，因此不把整个历史称为已清理。

Windows/RTX 4050 原机运行、真实实验质量及稳定发布就绪均为 **NOT_PROVEN**。
本地测试与 Ubuntu 的历史运行记录不能替代目标机验收。

## RTX 4050 首个问题

用户回传：全包文件校验和 AI 验证已经通过；PyTorch/CUDA 动态库已加载，但硬件
预检未完成。三次采样中 CPU 累计时间与读取字节数没有增长，七个线程处于等待。
这支持初始化停滞的判断，但尚不能区分 Torch 导入、CUDA 初始化、TensorRT 导入
或设备调用中的具体阻塞点，结论为 **PARTIAL_EVIDENCE**。

后续工作应记录每个初始化操作的进入、退出和耗时，在独立进程中设定可终止的等待
上限，并保留失败回执。超时上限不是固定等待时长，步骤完成后应立即继续。
源代码更新还必须维护离线包的文件完整性清单，不能仅覆盖源码后跳过校验。
