# VisionCortex RTX 4060 真实六路全链路运行回传

## 1. 运行身份

- 报告时间：
- 执行机器：
- 仓库绝对路径：
- Git 分支：
- Git commit SHA：
- `git status --porcelain`：
- 配置文件 SHA256：
- 两套 PT 权重 SHA256：
- 两套 TensorRT engine SHA256：
- 是否修改代码/配置/参数：否（若不是，停止运行并如实说明）

## 2. 输入与存储

- NAS 索引路径：
- 实验 ID：
- 视角数：
- 每路视频分片数：
- 每路时钟 CSV 数：
- 角色映射（1 路第一人称、5 路第三人称）：
- 输入模式：
- `copied_source_bytes`：
- `continuous_source_copies_created`：
- 本地缓存绝对路径：
- NAS staging 绝对路径：
- NAS 固定正式归档绝对路径：
- 运行前/后本地盘可用空间：
- 运行前/后 NAS 可用空间：

## 3. 网络与 NAS 性能

- NAS 服务器与共享：
- SMB Dialect：
- SMB 是否加密：
- 活跃网卡与链路速率：
- 粗扫 NAS/主机接收吞吐：均值 / P95 / 峰值 MiB/s
- 精扫 NAS/主机接收吞吐：均值 / P95 / 峰值 MiB/s
- 导出 NAS/主机发送吞吐：均值 / P95 / 峰值 MiB/s
- 粗扫累计读取字节：
- 精扫累计读取字节：
- 正式产出累计写入字节：
- 断流、重试、SMB 异常和等待：
- 证据文件：`JSON-Config-Files/resource_telemetry.json`

说明：主机网卡统计可能包含其他进程流量，必须同时列出 pipeline process-tree I/O；不要把两者混称为纯 NAS 吞吐。

## 4. 六路调度与推理性能

### 粗扫

- 同步分片波次数：
- 每波 6 路开始/完成时间：
- 同时活跃来源数（最小/最大）：
- CUDA/NVDEC 解码来源：
- CPU 解码来源：
- 第一人称 TensorRT backend/engine：
- 第三人称 TensorRT backend/engine：
- 第一人称 batch：请求值 / 实际均值 / 最大值 / 填充率
- 第三人称 batch：请求值 / 实际均值 / 最大值 / 填充率
- 队列：配置深度 / 实际峰值
- GPU Compute：均值 / P95 / 峰值
- NVDEC：均值 / P95 / 峰值
- 显存：均值 / P95 / 峰值 MiB
- GPU 功耗：均值 / P95 / 峰值 W
- GPU 温度：均值 / P95 / 峰值 °C
- CPU：均值 / P95 / 峰值
- 内存：均值 / P95 / 峰值

### 精扫与导出

- 精扫参与视角及每路原因：
- 精扫候选窗口数、总时长：
- 实际 batch、填充率和队列峰值：
- NVENC 导出利用率：
- 是否触发 OOM、batch 回退、CPU/NVDEC 动态收缩：
- 证据文件：`scan_runtime_coarse.json`、`scan_runtime_fine.json`、`resource_telemetry.json`

## 5. 总耗时与阶段耗时

- 请求到完成总耗时：
- 预处理耗时（目标 ≤ 1200 秒）：
- 是否达到 20 分钟目标：

| 阶段 | 开始 | 结束 | 耗时秒 | 备注 |
|---|---|---|---:|---|
| NAS 分片登记 | | | | 必须是零复制 |
| preflight | | | | |
| alignment | | | | |
| candidate_coarse | | | | |
| candidate_fine | | | | |
| candidate_audit | | | | |
| experiment_understanding | | | | |
| experiment_clips | | | | |
| key_materials | | | | |
| mllm | | | | |
| package | | | | |
| daily_report | | | | |

证据：`JSON-Config-Files/run_metrics.json`

## 6. Token

| 阶段 | 调用数 | 输入 Token | 缓存输入 Token | 输出 Token | 总 Token |
|---|---:|---:|---:|---:|---:|
| 实验组步骤理解 | | | | | |
| 关键素材步骤理解 | | | | | |
| 日报 | 0 | 0 | 0 | 0 | 0 |
| 全流程总计 | | | | | |

- 是否每次调用均为服务端 usage：
- 失败/重试调用及 Token：

## 7. 五个实验边界与连续性

| 序号 | 模型实验名 | 独立/连续 | global start | global end | 第一人称 MP4 | 第三人称 MP4 | 并排 MP4 | 人工参考匹配 |
|---:|---|---|---:|---:|---|---|---|---|
| 1 | | | | | | | | |
| 2 | | | | | | | | |
| 3 | | | | | | | | |
| 4 | | | | | | | | |
| 5 | | | | | | | | |

- 是否存在跨 15 分钟分片的实验片段：
- 若存在，最终 MP4 是否连续可播放：
- 边界偏差、漏检、误检或连续性争议：

## 8. 关键素材与步骤级理解

- 确认事件总数：
- 手部与物体接触：
- 物体移动：
- 液体移动：
- 容器状态变化：
- 设备面板操作：
- 第一/第三视角均有对应关键帧和连续关键片段的事件数：
- 当前步骤与下一步骤均有内容的事件数：
- uncertain/conflict 事件及原因：
- 统一事件 JSON 契约校验结果：

## 9. 日报与 PDF

- 日报 JSON：
- 日报 Markdown：
- 日报 HTML：
- 日报 PDF：
- 日报评价 JSON：
- PDF 页数与文件大小：
- 日报/PDF 是否反映五个实验、异常、耗时和 Token：

## 10. 验收与产物清单

- `evidence_package_eval.json` 是否通过：
- `Daily-Report-Eval.json` 是否通过：
- Experiment-Clips 文件数/总字节：
- Key-Materials 文件数/总字节：
- JSON-Config-Files 文件数/总字节：
- Lab-Daily-Reports 文件数/总字节：
- Professional-PDFs 文件数/总字节：
- 固定归档晋升结果：
- 上一版派生产物 history 位置：

## 11. 异常与真实问题清单

每项必须包含：症状、证据路径/时间戳、可能原因、影响、是否完成、下一步。不得为了“看起来成功”而省略错误。

## 12. 最终结论

- 全链路是否完成：
- 质量是否可人工验收：
- 预处理是否进入 20 分钟：
- 当前主要瓶颈：NAS / 解码 / TensorRT / CPU / 队列 / 导出 / MLLM / 其他
- 建议本地开发机下一步修改（只提建议，4060 节点不改代码）：
