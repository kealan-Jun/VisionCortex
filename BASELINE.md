# VisionCortex v0.1.0 Validation Baseline

冻结日期：2026-08-13

## 已冻结能力

- 输入路数动态，容量按至少 6 路、每路 3 小时设计。
- CSV 最近邻时间戳映射与视觉锚点残差校准。
- 第一人称/第三人称两套 21 类 YOLO、ByteSORT、粗扫与有界精扫。
- 独立实验与连续实验分组，只输出通过边界审计的有效视角。
- 每个实验组三份对齐 MP4、三份对应 JSON 和模型实验名称。
- 五类关键素材：手部与明确物体接触、物体移动、液体移动、容器状态变化、设备面板操作。
- 每个关键事件提供第一/第三/对齐视角的关键帧、关键片段、统一时间戳和规范化事件 JSON。
- 豆包 Seed 2.1 Pro 实验级与关键素材级细粒度理解，明确当前步骤、下一步骤、事实、推断、不确定项和矛盾。
- Web 动态上传、原视频双份留存、任务进度、正式 NAS 档案、模型理解、耗时与 Token 查看。
- 普通上传及固定基准的输入阶段、流水线阶段与真正端到端耗时记录。
- 固定基准 staging、evidence-package-eval 后晋升与上一版可恢复历史。

## 固定回归数据

- index experiment ID：`exp_20260810_144014_e918b762`
- 固定正式归档：`Y:\VisionCortexExperimentArchive\Six-View-Three-Hour-Experiment-2026-08-13`
- 输入：6 路，每路约 3.324 小时；1 路有效第一人称、5 路第三人称。
- 验收结果：5 个实验组、32 个关键事件、96 张关键帧、96 个关键片段。
- 媒体验收：15 个实验 MP4、96 个关键片段 MP4，ffprobe 失败 0。
- JSON/evidence-package-eval：失败 0。

## 基线性能与 Token

- 全量预处理：694.056768 秒（11 分 34.057 秒），不含模型理解。
- 一次完整连续运行：1121.339856 秒（18 分 41.340 秒）。
- 最终验收模型调用：37 次成功、0 次失败。
- 实验级理解：91,996 输入 + 9,903 输出 = 101,899 Token。
- 关键素材理解：100,525 输入 + 17,111 输出 = 117,636 Token。
- 总计：192,521 输入 + 27,014 输出 = 219,535 Token。

## 跨硬件开发约束

- 源码、`.pt` 权重、JSON 契约和测试在开发机与 4060 之间完全一致。
- 机器差异只进入配置文件、环境变量、遥测与目标机本地 `.engine`。
- `configs/default.yaml` 用于开发和通用回退；`configs/rtx4060-laptop-production.yaml` 用于 4060 正式运行。
- TensorRT engine 不进入 Git，必须在 4060 本机生成。
- 缓存身份绑定源码摘要、完整配置、权重 SHA256 与输入文件 identity，防止增强后误复用旧检测账本。
- API Key 不进入 Git 或交接包；4060 首次隐藏输入后保存为该 Windows 用户环境变量，后续自动复用。

## 后续增强准入

1. 从本基线 tag 创建功能分支。
2. 开发机执行全部测试、dry-run 和小样回归。
3. 4060 使用固定配置重跑六路基准。
4. 对比 5 个实验边界、跨视角一致性、32 个关键事件及当前/下一步理解。
5. evidence-package-eval、ffprobe、JSON 解析和 Token 账本全部通过后才合并。
6. 固定基准结果从 staging 晋升，失败运行不得覆盖正式档案。
