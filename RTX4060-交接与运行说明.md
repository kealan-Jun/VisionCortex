# VisionCortex RTX 4060 运行交接

> 4060 Codex 的强制运行任务书：`docs\RTX4060-Codex-真实六路全链路执行任务书.md`。
> 运行结束必须填写：`docs\RTX4060-真实六路运行回传模板.md`。4060 节点禁止改代码、
> 禁止跑开发测试；所有开发和参数决策回到本地开发机完成。

## 固定六路 3 小时基准

后续质量与性能验证固定复用以下任务，不再创建同名副本：

- 源实验 ID：`exp_20260810_144014_e918b762`
- 固定正式归档：`Y:\VisionCortexExperimentArchive\Six-View-Three-Hour-Experiment-2026-08-13`
- UNC：`\\192.168.66.149\video_database\VisionCortexExperimentArchive\Six-View-Three-Hour-Experiment-2026-08-13`
- 字段索引：`Y:\experiment_record_index.csv`
- 六路连续视频与时钟 CSV：`Y:\VisionCortexExperimentArchive\Six-View-Three-Hour-Experiment-2026-08-13\Original-Experiment-Videos\Normalized-Continuous`
- 原始分段：在字段索引中筛选上述实验 ID，查看 `rgb_file`、`frames_file`、`nas_segment_dir`。每路当前为 15 个分段。

六个连续输入目录为：

1. `lubancat-e8cc0cb3_cam01`（实际有效第一人称）
2. `orangepi5pro-ab748372_cam01`（第三人称）
3. `orangepi5pro-b439137c_cam02`（第三人称）
4. `orangepi5pro-d12a4719_cam01`（索引标作 first，但本实验遮挡，运行时按第三人称）
5. `orangepi5pro-fe0ec3fe_cam01`（第三人称）
6. `rk3588-ubuntu_cam01`（第三人称）

可运行 `deployment\rtx4060\05-查看NAS位置.ps1` 现场核对所有路径。

## 本地目录和 NAS 职责

> v0.2 分片原生约束：固定六路任务不再把每路 15 个分片拼成一条本地大视频。
> `rgb_file` 与 `frames_file` 直接构成六条虚拟时间轴，源数据复制量必须为 0。

- `Y:\experiment_record_index.csv`：固定任务的原始分片清单；输入直接从对应 NAS 路径读取。
- `Original-Experiment-Videos`：保留采集端现有的 15 分钟分片结构，不生成 82–88 GB 连续副本。
- `D:\VisionCortexLocal\Runtime`：阶段账本、运行日志、本地输出暂存和 Web PID。
- `D:\VisionCortexLocal\Cache`：按代码、配置、权重和输入指纹隔离的 YOLO 粗扫/精扫账本及断点缓存。
- `Y:\VisionCortexExperimentArchive`：正式结果根目录；固定任务的派生产物直接写隔离 staging，验收通过后晋升。
- `Y:\VisionCortexExperimentArchive\.VisionCortex-Run-Staging`：固定基准每次运行的隔离发布区；验收通过后才晋升到固定路径。
- `Y:\VisionCortexExperimentArchive\.VisionCortex-Run-History`：固定基准被替换的上一版派生产物，可用于回滚；原始视频目录不参与替换。
- `Y:\VisionCortexExperimentCache`：旧项目缓存仍保留，但本代码的 4060 默认不写这里。

缓存不会只按实验名盲目复用。`cache_identity.json` 同时绑定代码、配置、两套权重和输入文件身份；任何一项变化都会进入新的缓存目录。

## 首次部署

1. 把整个代码包复制到 4060 机器本地 SSD，例如 `D:\VisionCortex`。
2. 确认 NAS 映射为 `Y:`，并能访问上面的索引与归档。
3. 安装 Python 3.11/3.12、FFmpeg、最新可用 NVIDIA 驱动。
4. 在 PowerShell 中运行：

```powershell
Set-Location -LiteralPath 'D:\VisionCortex'
.\deployment\rtx4060\01-安装与检查.ps1
```

安装脚本会校验两套 21 类 YOLO 权重、安装 CUDA 版 PyTorch 与 TensorRT，并在 4060 本机导出两套 `.engine`。TensorRT engine 不随代码库分发，因为它与目标 GPU、TensorRT 和驱动环境绑定。

API Key 不写入代码、YAML、日志或 Git。安装脚本只在目标机安全提示输入，并保存到该 Windows 用户的 `ARK_API_KEY` 环境变量。

## 日常运行

启动 Web：

```powershell
.\deployment\rtx4060\02-启动Web.ps1
```

浏览器进入 `http://127.0.0.1:8000/#/home`，点击“重跑六路 3 小时基准”。它会复用固定归档，不新建任务目录。

也可不用 Web，直接运行：

```powershell
.\deployment\rtx4060\04-重跑固定六路基准.ps1
```

## 开发机与 4060 的协作方式

- 单一代码主线：所有机器使用同一个 Git 提交和输出 JSON 契约。
- 本机开发：运行单元测试、dry-run、Web 和小规模样本，不把本机性能数字当 4060 结果。
- 4060 验收：只使用 `configs\rtx4060-laptop-production.yaml`，运行固定六路基准并把正式结果写入固定 NAS 归档。
- 版本冻结：基线使用 Git tag；后续增强走功能分支和 PR。4060 只切到明确的 tag 或合并后的主线，不复制零散源文件。
- 硬件隔离：源码与 `.pt` 权重跨机器一致；`.engine`、缓存和性能报告只在对应运行节点生成。

## 耗时与 Token

每次运行均写入正式归档的 `JSON-Config-Files\run_metrics.json`：

- `preprocessing_sla`：预检、时间对齐、粗扫、精扫与边界审计；不含模型理解。
- `stage_durations`：每个阶段的起止时间和耗时。
- `tokens.experiment_groups`：实验片段步骤理解的输入/输出/总 Token。
- `tokens.key_materials`：关键素材步骤理解的输入/输出/总 Token。
- `web_ingest`、`web_end_to_end`：普通 Web 上传的接收留存时间与真正端到端时间。
- `nas_index_ingest`、`fixed_benchmark_end_to_end`：固定基准输入准备/复用与真正端到端时间。

当前本机验收基准：预处理 694.056768 秒（11 分 34.057 秒，不含模型理解）；最终验收包 37 次模型调用，共 219,535 Token，其中输入 192,521、输出 27,014。
