"""Optional training implementations, loaded only by explicit training requests.

Importing this package does not load a model framework. Concrete adapters may
depend on the pinned training stack and are not application startup modules.
"""
