@AGENTS.md

# Claude Code notes

- `AGENTS.md` is the authoritative shared project guidance. Keep common rules there and do not duplicate them in this file.
- On Windows, prefer native PowerShell for repository and filesystem work. Use Bash only for the repository's Linux deployment scripts and their syntax checks.
- Keep task-specific procedures in their existing canonical documents; do not expand this always-loaded file with run logs, temporary paths, or historical status.
- Do not edit project instruction files unless the user explicitly asks for an instruction change.
